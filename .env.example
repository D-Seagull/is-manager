# Base URL of the IS Fleet backend the app talks to.
# Local dev on a physical device: use your machine's LAN IP (not localhost),
# e.g. http://192.168.0.10:2024. Production: the Render URL.
EXPO_PUBLIC_API_URL=http://localhost:2024
